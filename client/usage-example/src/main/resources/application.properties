# --- Server Configuration ---
server.base.url=https://localhost:8443
server.endpoint.login=/jwt/tokens
server.endpoint.protected=/api/protected
server.endpoint.refresh=/jwt/refresh
# --- Client Settings ---
client.http.timeout.seconds=30
client.interactive=true